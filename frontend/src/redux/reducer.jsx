import AuthReducer from "./auth/reducer";
import MovieReducer from "./movie/reducer";

const reducers = {
  MovieReducer,
  AuthReducer,
  //Can we add upcoming reducer here.
};

export default reducers;
